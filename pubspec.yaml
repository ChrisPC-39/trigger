name: trigger
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.0.2
  hive_flutter: ^1.0.0
  path_provider: ^2.0.1
  animations: ^2.0.0
  shared_preferences: ^2.0.5
  provider: ^5.0.0
  day_night_time_picker: ^1.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.0.1
  build_runner: ^1.10.11

flutter:
  uses-material-design: true
